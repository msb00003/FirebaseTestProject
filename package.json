{
  "name": "top",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client-start": "cd client && npm run start",
    "client-build": "cd client && npm run build",
    "deploy": "npm run client-build && firebase deploy"
  }
}
